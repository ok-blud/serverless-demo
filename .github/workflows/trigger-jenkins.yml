name: Trigger Jenkins

on:
  push:
    branches: main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins build
        run: |
          curl -X POST http://ip10-0-61-5-d34h0dvtq0k1c7cornhg-8080.direct.lab-boris.fr/job/serverless-demo/build --user "leblud:123" 
